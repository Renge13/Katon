# Prompt Z close: merge round 2, then one gate ruling — Cowork → Code, 2026-09-13 (FINAL, all rulings in; Code executes the merges)

Cowork wrote this file into the device working tree. It is NOT durable until Code commits it: **commit it first, in PR C commit 1, before anything else in this doc.**

Reyner's verdict on walk v2 (2026-09-13, verbatim rulings below). Round 2 is CLOSED under the two-round cap. Nothing here is a third round: two merges, one gate change that removes a cause for pair readings, one register entry. Reyner has ruled everything in this doc; do not stop to ask him for a decision it already carries.

Four checks, cited: (1) figures below were recomputed by Cowork on the staged tree (`count-seed-runs.mjs --falsify` REPRODUCED 13/42; control on the example text returned 54 runs); (2) every assertion named has a red shown first; (3) the gate change EXEMPTS a check for one reading kind, it adds none; (4) customer: the clash-pair reader stops being the most likely to receive the floor.

## RULINGS (Reyner, 2026-09-13) — record verbatim in the repo, Code commits
R1. MERGE PR B. Round 2 resolves provenance (`kamu`/`dia`), the opening via engine, the penutup addressing the reader. The B3 example duplicating on produces-pairs is an accepted trade-off against baseline or floor.
R2. `bukan X, melainkan Y` is VALID — not hedging. "Konstruksi ini adalah bentuk kontras alami dan presisi dalam bahasa Indonesia, bukan kalimat ragu-ragu. Penyebab clash pair sering floor adalah validatornya yang terlalu kaku. Penyebabnya ada di gate, bukan di kalimat."
R2-SCOPE (final). PAIR-ONLY. Retain the strict `bukan X, tapi/melainkan Y` ban on the MIRROR to prevent AI copy tropes in single-person readings (the 2026-08-06 gallery ruling stands there). Exempt `style.hedge_construction` exclusively for compat readings (`semanticJson.kind === 'pair'`) so the mandatory p2_reframe move ("bukan vonis/penentu..., melainkan...") does not trigger false-positive gate rejections.
R3. B3 last sentence: FULL STOP ratified (mobile 375px readability). The committed prompt (`compat-renderer-prompt.txt:59`) already carries it — no change.
R4. DEFERRED REGISTER: B3 example duplication on produces-pairs. P1 variation is a later iteration, not a release hold.
R5. Code executes the merges and the retarget (Reyner, 2026-09-13); Reyner does not click anything.

## 1. Merges — CODE DOES THESE (`gh`), in this order, plain merge commits, never squash
1. Confirm CI green on #116 and #118 at their current heads; quote the check run ids.
2. `gh pr merge 116 --merge` (seeds, `feat/compat-seeds` → main).
3. `gh pr edit 118 --base main`, confirm the diff is now round 2 alone (prompt + two log-only checks + walk artifacts + count script; no glossary.json change), then `gh pr merge 118 --merge`. Two revertable content merges.
4. On main after both: `npm test` with the exit code captured explicitly — `npm test; echo EXIT=$?` — never through a pipe into `tail`. Confirm `STAGE6_VERSION` is `1.24.0` and prompt hash `6e7b2997b97c40a3` on main. Quote both.
5. Report the two merge commit hashes.

## 2. PR C — gate change, ISOLATED (CLAUDE.md: gate changes ship alone; one STAGE6_VERSION bump)
Branch from main after step 1.4.
Commit 1 (rulings record, no behaviour): commit THIS FILE at `docs/prompts/Z-close-2026-09-13.md`; add R2 + R2-SCOPE verbatim to `lib/validate/blocklist.json` as a `_pair_scope`-style note on `hedge_construction`, dated; add a dated row in `docs/PROGRESS.md` MEASUREMENTS; add the deferred-register row in section 4.
Commit 2 (the gate change, nothing else staged — read `git status` before the subject line):
- Pair exemption for `style.hedge_construction`, implemented where `_pair_scope` is already read (`lib/validate/style.js`), keyed on `semanticJson.kind === 'pair'`. Mirror path byte-identical. Pattern and `bukan berarti` carve-out untouched.
- `STAGE6_VERSION` 1.24.0 → 1.25.0 in the same commit.
- Tests, `tests/stage6-validation.spec.mjs`: the existing mirror assertions at :925-961 and :1055-1058 STAY as they are (they now prove the mirror is unchanged). ADD, in `tests/compat-stage6-pair.spec.mjs`: (a) a pair reading whose P2 block carries "Kursi pasangan kalian saling berbenturan, yang menandakan titik tuntutan kesadaran ekstra, bukan vonis ketidakcocokan." (2x6 round 2 literal) and one carrying "Ini bukan penentu kegagalan, melainkan tanda ..." (Y-1 floored-draw literal) → `style.hedge_construction` NOT in the checks; (b) the same text in a MIRROR block → still rejected. SHOW RED FIRST: run (a) against the pre-change gate, quote the failure in the commit message.
- `pair.reframe_missing` UNTOUCHED.
Open PR C; do NOT merge it. Reyner merges PR C after Cowork reads the n=20 artifact (section 3). This is the one merge that stays his, because it changes what Stage 6 accepts.

## 3. Measure once, then stop
`n=20`, two draws, same harness, gate 1.25.0, prompt unchanged (`6e7b2997b97c40a3`). Report with fixed denominators: floor rate overall AND floor rate on pairs carrying `p2_reframe` (clash/harm/punishment) separately — that is the number R2 is meant to move (round 2: Y-1 floored 3 of 4 draws, all three visible literals were `hedge_construction` + `hedging`). Also report `style.hedging` literals with the block and seed field they landed in, because the round-2 report said "meaning sentence" and the walk artifact's three literals are all the p2_reframe DAILY seed (`gesekan cenderung dipendam sampai menjadi percikan`). Write the artifact under `docs/qa/` and give its path; Cowork reads the artifact, not the summary. No prompt edit follows from this measurement in this PR; it is a record.

## 4. DEFERRED REGISTER entry (docs/PROGRESS.md), Code writes in PR C commit 1
| B3 example duplicated on produces-pairs | The round-2 prompt's worked example (Samudra→Taman, `a_produces_b`) is reproduced with element names swapped whenever the pair's P1 has the example's input shape: 1x2 and Y-1 P1 byte-identical in walk v2; 38 nine-word runs vs target 0 (`scripts/count-seed-runs.mjs --column "round 2"`). Controls-pairs (2x6) did not copy. Blocks with no demonstration (P4, P5) remain seed transcriptions (9/42 vs target ≤3). | Accepted by Reyner 2026-09-13 (R1): richer than baseline, and the copied prose is his ruled text. | Would close: a template-level P1 in a later render prompt, or a second example of a different cycle shape, measured with the same script. Not a release hold. |
Also log (PROGRESS note, no register row): `p1_stem_relation` is not a required point — a model that skips P1 passes coverage.

## 5. Explicitly NOT in this close
No prompt changes. No B4 rework (hedging on `cenderung` stays a logged observation; the floor loss was inside draw variance, 4/10 vs 1/10 from one prompt). No mirror gate change. No third round. No merge of PR C by Code.
