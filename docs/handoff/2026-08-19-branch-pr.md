Four unrelated bodies of work, none of it user-facing: the **payload join and floor**,
the **front door**, the **card**, and the **Stage 6 gate** — plus the probe set they were
measured with. Nothing here changes what a reader gets, because the rendered pipeline
still serves nobody: `/api/mirror/[token]` 404s without `MIRROR_PREVIEW_TOKEN` and is
linked from nowhere.

**Read in this order.** The card track is the largest and the most finished:
`npm run audit:card-contrast` exits 0 for the first time, 8 findings to 0. The gate
track is the smallest and the most consequential: `STAGE6_VERSION` 1.9.0 → 1.10.0
alongside the two changes that should have moved it, and the regeneration budget set
to 2 from measurement. Three things below are worth a reviewer's attention on their own
and each is linked to its evidence: an exemption list that could not be tested while it
was empty, a ruling made on a premise that turned out false, and two different gates
that shared one version label for a day.

**Supersedes #44**, whose four commits are the first track here, unchanged.

Full suite green at `5fc9292`: 19 test scripts (326 assertions), `lint`, `typecheck`,
`check:copy`, `audit:card-contrast`, `audit:card-budget`.

---

## Why one PR for four tracks

The branch was called `feat/palace-domain-join`, which described its first four commits
and none of the other twenty-two. **Splitting them retroactively costs a day and buys
tidiness, so it was not done** (Reyner, 2026-08-19). An honest description of a mixed
branch beats a dishonest name on a clean one, and the table below is that description.

Nothing here changes what a reader gets. **The rendered pipeline still serves nobody**
— `/api/mirror/[token]` 404s without `MIRROR_PREVIEW_TOKEN` and is linked from
nowhere — so none of the four tracks needs to ship separately, and none is urgent.

| # | track | commits | what it is |
|---|---|---|---|
| 1 | **The payload join and the floor** | `9b2b6b2` → `ea36be9` (4) | `pilar.*.domain_id` had been data read by nothing since tranche 1; every palace now carries its life domain. `ENGINE_VERSION` 0.4.3 → 0.4.4-stage3. The module-assembly floor now states WHERE a relation sits, not only what it is. |
| 2 | **The front door** | `d81434a` → `e4d277b` (5) | Native date and hour input, with the minute asked for only where a solar term can fall inside the hour. The WhatsApp checkout field removed, because the promise attached to it had no sender behind it. Front-door copy in Reyner's ruled words. A way back from the Xendit tab. |
| 3 | **The card** | `2a3d8bf` → `21d690a` (9), plus `053250f` and `0bc8eff` | Card A and Card B built to the 08-01 / 08-03 rulings, then rebuilt as 1a / 1e to the polish spec. Ten colour tokens approved, the accent floor frozen, WCAG AA enforced by a DOM walk over the real markup, the export rastered and read back, Archivo loaded, the hanzi face subsetted and carried inside the export object. |
| 4 | **The Stage 6 gate** | `0280278` | `style.adverbial` deleted, `style.hedging`'s `mungkin` replaced by a clause-subject test, and `STAGE6_VERSION` bumped 1.9.0 → 1.10.0 in the same commit — which is the rule the commit installs. |
| — | **Instruments** | `12c3401` | Five probes and the QA dump, mostly built by earlier sessions and left uncommitted. Committed under a message that names them rather than riding inside a narrower subject. |

## What is worth a reviewer's attention

**`npm run audit:card-contrast` exits 0 for the first time.** `FAIL - 8 finding(s)` →
`PASS`. Two `rendered` findings closed by removing the day cell's brass wash, four
`sheen` findings closed by measuring brass text against the surface it is actually
drawn on, and the remaining two excused as a named ruling with their ratios still
printed on every run.

**One exemption list stopped being empty, and that exposed a bug in the report.**
`SHEEN_EXEMPT` went from `[]` to `['乙','丙']`, and 乙 and 丙 immediately printed
`clears` in the very section that exists to keep their failure visible — because the
report row was recorded inside the `!exempt` branch. It is the same defect as
`21d690a`, one report over, and it was **untestable while the list was empty**. The
generalisation is in the ledger: an exemption mechanism cannot be tested until
something is put in it, so every sibling list has the same latent bug today.

**A ruling was made on a premise I got wrong, and the correction is in the code.**
The brass-text fallback was split per card on the stated grounds that pooling it
"made Card A pay for Card B's finish". Card A draws no brass text at all — all three
roles reading `brassText` are Card B only. The split is kept on a rebuilt argument
and changes not one pixel; a test pins Card A's zero-brass fact so the guard cannot
rot. Ledger row 39.

**Two gates both stamped `1.9.0` for a day.** The gate changes landed without the
bump, so the only thing separating them is file mtime. `render_cache` was read
before anything was ruled: zero rows under either, six rows at `1.8.0` under a
superseded `ENGINE_VERSION`, nothing served, nothing purged. Neither probe artifact
was re-stamped — the reconciliation is a PROGRESS row that says mtime is the evidence.

## What this branch does NOT do

- **It does not promote the mirror.** Its four named preconditions are unchanged.
- **It does not set a retry depth.** The depth-6 sweep and the free erosion read are
  both in `docs/qa/`; depth stays at 1 until Reyner rules.
- **It does not close the rule-23 bracket hole.** `Aspek Pemikir (Indirect Resource)`
  → `Aspek Pemikir` passes the gate, and the bracket rate falls 99% → 82% → 66% by
  attempt number. Measured deliberately and not fixed: a coverage check changes what
  the gate rejects, so it needs its own measurement and its own version bump.
- **It does not touch the reader's wait**, which turns out not to exist yet: the
  funnel's 2.5s pause is a designed pause, not a wait on work, and the LLM path is
  fenced. The measured 7.6s p50 arrives at promotion, not before.

## On #44, and on the branch name

**#44 is closed as superseded, not abandoned.** Its four commits are the first track
here, byte-identical and unrebased, so its diff is contained in this one and the closed
PR stays readable. It was a coherent 4-commit renderer PR; folding twenty-two unrelated
commits into it would have destroyed that without gaining anything, and it had no
reviews to preserve.

`origin/feat/palace-domain-join` is **deliberately not deleted** — a closed PR pointing
at a deleted branch loses its diff. It stays where it is, 21 commits behind, as #44's
record.
