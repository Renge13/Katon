# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Next.js
.next
out
next-env.d.ts

# Env (secrets — server-only keys must never be committed)
.env
.env*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local config (settings are local; shared slash commands are versioned)
.claude/*
!.claude/commands/

# Claude Design reference bundle (kept locally as reference, not shipped/linted)
Katon Design System/

# Stage 6 measurement output. Numbers belong in docs/PROGRESS.md as a dated
# observation (rule 8); the raw run logs and blind-judge pairs do not belong in git.
reports/
